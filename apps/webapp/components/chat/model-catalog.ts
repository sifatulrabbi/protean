/**
 * @deprecated Import from `@protean/model-catalog` instead.
 */
export {
  getProviderById,
  getModelById,
  getModelReasoningById,
  resolveClientModelSelection,
} from "@protean/model-catalog";

/**
 * @deprecated Import from `@protean/model-catalog` instead.
 */
export type {
  AIModelEntry,
  AIModelProviderEntry,
} from "@protean/model-catalog";

/**
 * @deprecated Use `resolveClientModelSelection` from `@protean/model-catalog` instead.
 */
export { resolveClientModelSelection as resolveModelSelection } from "@protean/model-catalog";
